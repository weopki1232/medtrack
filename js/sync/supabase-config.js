// ── Supabase configuration ─────────────────────────────────────────────────────
// Fill these in from your Supabase project (Settings → API). The anon key is
// safe to ship publicly — row-level security restricts every user to their own
// rows. Leave both empty to run the app fully offline with no account features.
var SUPABASE_URL = '';
var SUPABASE_ANON_KEY = '';
